mutation setCooperativeUnionData(
  $id: ID!
  $data: CoopUnionInstitutionInformationInput
) {
  members {
    cooperativeUnion(id: $id) {
      add(id: $id) {
        institutionInformation(data: $data) {
          recordId
        }
      }
    }
  }
}
