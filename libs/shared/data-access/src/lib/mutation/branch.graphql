mutation setBranchData($id: ID!, $data: BranchInput!) {
  settings {
    general {
      branch {
        add(id: $id, data: $data) {
          record {
            id
          }
          error {
            ... on BranchAddInvalidDataError {
              error
            }
          }
        }
      }
    }
  }
}
