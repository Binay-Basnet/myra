mutation setOrganizationData($data: OrganizationInput!) {
  settings {
    general {
      organization {
        initialSetup(data: $data) {
          recordId
          error {
            ...MutationError
          }
        }
      }
    }
  }
}
