query getRecentTransactions {
  eBanking {
    transaction {
      recent {
        id
        name
        transactionType
        date
        transactionDirection
        amount
      }
    }
  }
}
