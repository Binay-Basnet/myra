mutation setIssueNewSlip($accountId: ID!, $branchId: String!,  $count: Int!) {
  withdrawSlip {
    issueNew(branchId: $branchId, pickupMethod: MARKET_REPRESENTATIVE, accountId: $accountId, count: $count) {
      recordId
      error {
        ...MutationError
      }
    }
  }
}
