query getInventoryWarehouseDetails($id: ID!) {
  inventory {
    warehouse {
      getWarehouseDetails(id: $id) {
        data {
          id
          name
          phoneNumber
          address
          branchId
          branchName
        }
        error {
          ...QueryError
        }
      }
    }
  }
}
