query getAuditLogList($filter: AuditLogFilters) {
  auditLog {
    humanize(filter: $filter) {
      __typename
      ... on AuditLogHumanizeResult {
        data {
          timestamp
          narration
          extraData
        }
      }
    }
  }
}
