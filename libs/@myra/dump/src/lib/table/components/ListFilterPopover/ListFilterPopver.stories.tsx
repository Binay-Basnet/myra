import { Meta } from '@storybook/react';

import { ListFilterPopover } from './ListFilterPopver';

export default {
  component: ListFilterPopover,
  title: 'Old Dump /Table / List Filter Popover',
} as Meta;
