import ImportLayout from '../../../components/SettingsLayout/ImportLayout';
const Import = () => {
  return <> Import</>;
};

export default Import;
Import.getLayout = function getLayout(page) {
  return <ImportLayout>{page}</ImportLayout>;
};
